mutation HolochainCreateCommunityMutation (
  $slug: String!,
  $name: String!
) {
  createCommunity(
    data: {
      slug: $slug,
      name: $name
    }
  ) @client {
    id
    slug
    name
  }
}
