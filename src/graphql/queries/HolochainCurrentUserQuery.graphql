query HolochainCurrentUserQuery {
  me @client {
    id
    name
    avatarUrl
    isRegistered
    memberships @client {
      id
      community {
        id
        name
        slug
      }
    }
  }
}
